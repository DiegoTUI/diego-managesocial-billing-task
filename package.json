{
  "name": "diego-managesocial-billing-task",
  "version": "0.0.1",
  "private": true,
  "main": "app.js",
  "scripts": {
    "start": "node ./bin/www",
    "test": "npm run jshint && mocha --recursive test",
    "test:unit": "mocha --recursive test/middleware test/models test/routes",
    "test:integration": "mocha --recursive test/integration",
    "jshint": "grunt"
  },
  "dependencies": {
    "body-parser": "^1.13.3",
    "express": "^4.13.3",
    "morgan": "^1.6.1",
    "node-env-file": "0.1.8",
    "request": "^2.60.0",
    "request-promise": "^4.1.1"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "debug": "^2.2.0",
    "grunt": "^1.0.1",
    "grunt-contrib-jshint": "~1.0.0",
    "mocha": "^3.0.2"
  }
}
